extern mod os { path= "../../_gal/mods"; }
mod envs {
    env default {}
}
mod main   {
  flow ai_test{
    gx.ai_chat("1+1=?");
  }

  flow ci_msg {
    gx.cmd("git diff > code_diff.txt");
    gx.ai_chat( "Generate git commit message", prompt_file : "./code_diff.txt");
  }

}
